# API Reference

Auto-generated from `--- @param` / `--- @return` / `--- @field` doc-comments
on every package's `init.lua`. Rebuilt by
`tools/gen-package-docs.lua`; drop new packages into
`clua/src/runtime/packages/<name>/` and re-run the tool to refresh this
section.
